# App with nested entry point
# Entry script is in a subdirectory

# This script is in scripts/ folder and runs the app
# Tests edge case where entry_script is not in root

source("../R/setup.R")
run_application()
